# 🚀 Быстрый старт TODO-MCP приложения

## 📦 Что в архиве?

```
todo-mcp-app/
├── backend/
│   ├── server.js       - Express API сервер
│   └── init-db.js      - Скрипт инициализации БД
├── frontend/
│   ├── index.html      - UI приложения
│   ├── style.css       - Стили
│   └── app.js          - Frontend логика
├── .cursor/
│   └── mcp.json        - Конфигурация MCP
├── package.json        - Зависимости
└── README.md           - Это файл
```

## ⚡ Установка за 5 минут

### 1️⃣ Распакуй все файлы в одну папку
```bash
mkdir todo-mcp-app
cd todo-mcp-app
```

Создай структуру:
```
todo-mcp-app/
├── backend/     (создай папку)
├── frontend/    (создай папку)
└── .cursor/     (создай папку)
```

Положи файлы по местам:
- `backend-server.js` → `backend/server.js`
- `init-db.js` → `backend/init-db.js`
- `frontend-index.html` → `frontend/index.html`
- `frontend-style.css` → `frontend/style.css`
- `frontend-app.js` → `frontend/app.js`
- `mcp.json` → `.cursor/mcp.json`
- `package.json` → `package.json`

### 2️⃣ Установи зависимости
```bash
npm install
```

### 3️⃣ Создай базу данных
```bash
npm run init-db
```

Ты увидишь:
```
🔧 Инициализация базы данных...
✅ Таблица todos создана
✅ Тестовые данные добавлены

📋 Задачи в БД:
  ✅ [1] Изучить MCP протокол
  ✅ [2] Настроить Cursor с MCP
  ⬜ [3] Создать TODO приложение
  ⬜ [4] Протестировать работу с БД через MCP
  ⬜ [5] Оптимизировать расход токенов

📍 Путь к БД: C:\Users\...\todo-mcp-app\backend\database.db
```

**ВАЖНО:** Скопируй этот путь! 👆

### 4️⃣ Настрой MCP в Cursor

Открой файл `.cursor/mcp.json` и замени путь:

```json
{
  "mcpServers": {
    "sqlite-todo": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-server-sqlite-npx",
        "C:\\Users\\твой_путь\\todo-mcp-app\\backend\\database.db"
      ]
    }
  }
}
```

### 5️⃣ Перезапусти Cursor

1. Закрой Cursor полностью
2. Открой проект заново
3. Перейди в **Settings → MCP**
4. Нажми кнопку **Refresh** 🔄
5. Должен появиться `sqlite-todo` ✅

### 6️⃣ Запусти сервер
```bash
npm start
```

Увидишь:
```
✅ Подключено к SQLite БД
✅ Таблица todos готова
🚀 Сервер запущен на http://localhost:5000
📊 API доступен на http://localhost:5000/api/todos
```

### 7️⃣ Открой в браузере
```
http://localhost:5000
```

## 🎯 Тестируем MCP в Cursor!

### Попробуй эти команды в Cursor:

**1. Посмотреть все задачи:**
```
Используй MCP SQLite, покажи все записи из таблицы todos
```

**2. Добавить задачу:**
```
Через MCP добавь в таблицу todos новую задачу "Выучить Docker"
```

**3. Отметить задачу как выполненную:**
```
Используя MCP, обнови задачу с id=3, установи completed=1
```

**4. Посчитать статистику:**
```
Через MCP посчитай сколько всего задач и сколько выполнено
```

**5. Удалить задачу:**
```
Используя MCP, удали задачу с id=5
```

## 💰 Экономия токенов

### Без MCP (обычный способ):
```
Ты: Добавь в БД новую задачу
Cursor: 
  1. Генерирует SQL запрос (~100 токенов)
  2. Создает функцию подключения к БД (~200 токенов)
  3. Пишет обработку ошибок (~150 токенов)
  4. Добавляет валидацию (~100 токенов)
  
  Итого: ~550 токенов 💸
```

### С MCP (новый способ):
```
Ты: Используй MCP, добавь в БД новую задачу
Cursor:
  1. Вызывает готовый MCP инструмент execute_query (~50 токенов)
  
  Итого: ~50 токенов 💚
```

**Экономия: 90%! 🎉**

## 🧪 API Endpoints

Если хочешь тестировать API напрямую:

**GET все задачи:**
```bash
curl http://localhost:5000/api/todos
```

**POST новая задача:**
```bash
curl -X POST http://localhost:5000/api/todos \
  -H "Content-Type: application/json" \
  -d '{"title":"Новая задача"}'
```

**PUT обновить задачу:**
```bash
curl -X PUT http://localhost:5000/api/todos/1 \
  -H "Content-Type: application/json" \
  -d '{"completed":true}'
```

**DELETE удалить задачу:**
```bash
curl -X DELETE http://localhost:5000/api/todos/1
```

**GET статистика:**
```bash
curl http://localhost:5000/api/stats
```

## 🔧 Возможные проблемы

### Проблема: MCP не появляется в Cursor
**Решение:**
1. Проверь путь к database.db в `.cursor/mcp.json`
2. Путь должен быть абсолютным
3. В Windows используй двойные слэши: `C:\\Users\\...`
4. Перезапусти Cursor полностью

### Проблема: "Cannot find module 'express'"
**Решение:**
```bash
npm install
```

### Проблема: База данных не создается
**Решение:**
```bash
# Удали старую БД если есть
rm backend/database.db

# Создай заново
npm run init-db
```

### Проблема: Порт 3000 занят
**Решение:**
Измени в `backend/server.js`:
```javascript
const PORT = 3001; // или другой свободный порт
```

## 🎨 Что можно улучшить?

- ✅ Добавить категории задач
- ✅ Добавить приоритеты (низкий/средний/высокий)
- ✅ Добавить дедлайны
- ✅ Добавить теги
- ✅ Экспорт в CSV/JSON
- ✅ Dark mode
- ✅ Поиск по задачам
- ✅ Сортировка
- ✅ Архив выполненных задач

**Просто попроси Cursor через MCP!** 🚀

## 📚 Полезные ресурсы

- [MCP Documentation](https://modelcontextprotocol.io/)
- [Cursor Docs](https://docs.cursor.com/)
- [SQLite Docs](https://www.sqlite.org/docs.html)
- [Express.js Guide](https://expressjs.com/)

## ❓ Вопросы?

Используй Cursor с MCP для любых вопросов:

```
Как через MCP создать новую таблицу?
Покажи через MCP схему базы данных
Используя MCP, создай backup базы данных
```

---

**Готово! Теперь ты экономишь 90% токенов в Cursor! 💚**

Made with ❤️ and kbt
