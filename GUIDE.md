# TODO-приложение с MCP SQLite сервером для Cursor

## 📋 Что мы создадим
Простое TODO-приложение с:
- Frontend на HTML/CSS/JavaScript
- SQLite база данных
- MCP сервер для работы с БД через Cursor

## 🎯 Структура проекта

```
todo-mcp-app/
├── .cursor/
│   └── mcp.json              # Конфигурация MCP для Cursor
├── frontend/
│   ├── index.html            # Главная страница
│   ├── style.css             # Стили
│   └── app.js                # Логика frontend
├── backend/
│   ├── server.js             # Express сервер API
│   └── database.db           # SQLite база данных
├── package.json              # Зависимости проекта
└── README.md                 # Инструкции
```

## 🚀 Пошаговая установка

### Шаг 1: Создай папку проекта
```bash
mkdir todo-mcp-app
cd todo-mcp-app
```

### Шаг 2: Инициализируй Node.js проект
```bash
npm init -y
```

### Шаг 3: Установи зависимости
```bash
npm install express sqlite3 cors
npm install -g mcp-server-sqlite-npx
```

### Шаг 4: Создай структуру папок
```bash
mkdir frontend backend .cursor
```

### Шаг 5: Настрой MCP в Cursor

Создай файл `.cursor/mcp.json` с таким содержимым:

```json
{
  "mcpServers": {
    "sqlite-todo": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-server-sqlite-npx",
        "C:/полный/путь/к/проекту/backend/database.db"
      ]
    }
  }
}
```

**ВАЖНО:** Замени `C:/полный/путь/к/проекту` на реальный путь к твоему проекту!

### Шаг 6: Создай базу данных

Создай файл `backend/init-db.sql`:

```sql
CREATE TABLE IF NOT EXISTS todos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    completed BOOLEAN DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Добавим тестовые данные
INSERT INTO todos (title, completed) VALUES 
    ('Изучить MCP', 0),
    ('Создать TODO приложение', 0),
    ('Протестировать в Cursor', 0);
```

### Шаг 7: Инициализируй БД
```bash
sqlite3 backend/database.db < backend/init-db.sql
```

Или используй Cursor с MCP:
1. Открой проект в Cursor
2. Перейди в Settings → MCP
3. Нажми Refresh
4. Теперь Cursor может работать с твоей БД!

## 💡 Как экономить токены в Cursor

### Используй MCP вместо написания SQL вручную

**БЕЗ MCP (тратит много токенов):**
```
Пользователь: Добавь в БД новую задачу
Cursor: [генерирует SQL код, создает функции, пишет обработчики ошибок... много токенов]
```

**С MCP (экономит токены):**
```
Пользователь: Добавь в БД новую задачу
Cursor: [использует готовый MCP инструмент execute_query... мало токенов]
```

### Примеры команд для Cursor с MCP

1. **Посмотреть все задачи:**
   ```
   Используй MCP SQLite, покажи все задачи из таблицы todos
   ```

2. **Добавить задачу:**
   ```
   Через MCP добавь в todos новую задачу "Купить молоко"
   ```

3. **Обновить задачу:**
   ```
   Используя MCP, отметь задачу с id=1 как выполненную
   ```

4. **Удалить задачу:**
   ```
   Через MCP удали задачу с id=2
   ```

## 🎨 Файлы для создания

Теперь создай эти файлы (Cursor поможет с кодом через MCP!):

### 1. `backend/server.js` - Express API сервер
- Endpoints: GET /todos, POST /todos, PUT /todos/:id, DELETE /todos/:id
- Использует sqlite3 для работы с БД

### 2. `frontend/index.html` - UI приложения
- Форма для добавления задач
- Список задач с чекбоксами
- Кнопки удаления

### 3. `frontend/style.css` - Стили
- Простой, чистый дизайн
- Responsive layout

### 4. `frontend/app.js` - Frontend логика
- Fetch API для общения с backend
- DOM манипуляции

### 5. `package.json` - Скрипты
```json
{
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js"
  }
}
```

## 🧪 Тестирование

1. Запусти сервер:
   ```bash
   npm start
   ```

2. Открой браузер: `http://localhost:3000`

3. Протестируй через Cursor:
   - "Покажи все задачи из БД через MCP"
   - "Добавь 5 тестовых задач через MCP"
   - "Посчитай сколько задач выполнено через MCP"

## 🎯 Что дальше?

После базового проекта можешь добавить:
- ✅ Категории задач
- ✅ Приоритеты
- ✅ Дедлайны
- ✅ Поиск и фильтры
- ✅ Статистику

Все это легко сделать через MCP - просто спроси Cursor!

## 💰 Экономия токенов - Summary

| Действие | Без MCP | С MCP | Экономия |
|----------|---------|-------|----------|
| Создать таблицу | ~500 токенов | ~50 токенов | 90% |
| Добавить запись | ~300 токенов | ~30 токенов | 90% |
| Сложный запрос | ~800 токенов | ~100 токенов | 87% |

**Итого:** MCP экономит до 90% токенов при работе с базой данных! 🎉

## 📚 Полезные ссылки
- [MCP SQLite NPM](https://www.npmjs.com/package/mcp-server-sqlite-npx)
- [Cursor MCP Docs](https://docs.cursor.com/context/model-context-protocol)
- [SQLite Documentation](https://www.sqlite.org/docs.html)

---

**Готов начать?** Скажи мне, и я помогу создать все файлы! 🚀
